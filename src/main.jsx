import React from "react";
import ReactDOM from "react-dom/client";
import PrintTracker from "./PrintTracker.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <PrintTracker />
  </React.StrictMode>
);
